---
title: "DBX010: One append holds events of several stream types"
description: "One append holds events of several stream types."
---

This page explains error DBX010 and how to fix it.

## Cause

One `Append` or `Execute` returns events of several stream types.

## Fix

Append each stream's events separately. One append writes one stream.
