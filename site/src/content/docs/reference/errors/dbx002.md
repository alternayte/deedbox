---
title: "DBX002: No database provider is configured"
description: "No database provider is configured."
---

This page explains error DBX002 and how to fix it.

## Cause

`AddDeedbox` has no database provider.

## Fix

Call `UsePostgres(...)` or `UseSqlServer(...)` in `AddDeedbox`.
